# ColorTiming

ColorTiming 项目业务代码根目录，按应用、领域、基础设施和表现层组织。

直接脚本：`AssemblyInfo.cs`。

子目录：`Application`、`Bootstrap`、`Domain`、`Infrastructure`、`Input`、`Presentation`、`Settings`。

修改本目录代码后，应执行 Unity 编译和对应测试。
