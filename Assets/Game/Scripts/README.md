# Game Scripts

汇总项目通用扩展与 ColorTiming 业务代码，是业务接入 GF_X 框架的脚本根目录。

直接脚本：`HotfixEntry.cs`、`RuntimeObjectNaming.cs`。

子目录：`ColorTiming`、`Common`、`DataTable`、`Entity`、`EventArgs`、`Extension`、`Network`、`Procedures`、`ScriptableObject`、`UI`。

修改本目录代码后，应执行 Unity 编译和对应测试。
