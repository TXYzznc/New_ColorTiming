# 方案探索派发单

项目在 OpenSpec 建立前，将 `DispatchTemplate.md` 的副本放入项目协作区的 `Active/<dispatch-id>.md`。该副本只承载当前方案探索；不得在框架基线提交具体派发内容。

专业窗口取得用户确认并创建 OpenSpec 后，将派发单移动到 `openspec/changes/<change-id>/dispatch.md`，随后与该 change 一起归档。不要建立独立的重复历史副本。

除制作人窗口外，其他专业窗口默认只读活动派发目录，并通过回传请求更新中央占用或派发状态。
