# 方案讨论增量归档：框架协作增量同步

> 记录检查点而非原始聊天流水。它不代替本次变更的 OpenSpec artifacts。

## 检查点

- 日期：2026-08-26
- 讨论轮次：3
- 派发/OpenSpec：将创建 `adopt-git-integration-workflow`
- 维护者：主对话

## 已确认决策

- 当前项目需要补齐 `git-integration` 的实际角色定义，使常规 Git 暂存与提交有唯一受控执行主体。
- 同步采用智能增量合并：只纳入当前项目确实需要的框架能力，保留业务 Docs、OpenSpec、配置和所有未提交的重构修改；禁止整仓合并或覆盖式同步。
- 即使当前以单窗口重构为主，也立即启用专职 Git 集成窗口；后续客户端、技术美术、测试等长期岗位窗口共享仓库时，常规提交必须经该窗口执行。

## 明确排除

- 不合并外部仓库 Git 历史，不 cherry-pick 其提交。
- 不同步外部框架仓库中的产品无关变更目录作为当前项目的业务变更。
- 不修改 ColorTiming 的场景、Prefab、运行时代码或当前重构实现。

## 待决事项与影响

- 待决：无阻塞决策；具体 Git 集成窗口的实际 thread 注册在用户创建长期窗口时按本机注册表配置。
- 受影响工作/文档：`.claude` agent 源、Dispatch 文档、框架纯度审计及其测试、生成的 `.codex` 配置。
- 下次行动：建立并实施 OpenSpec，最后完成 FSR 子模块的本机初始化验证。

## 同步记录

- 已更新的稳定文档：本归档文件。
- 已通知制作人动作：不需要；当前由主对话直接与用户收敛。
