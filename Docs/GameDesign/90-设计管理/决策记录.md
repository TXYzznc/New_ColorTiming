# 决策记录

| ID | 日期 | 决策 | 理由 | 影响 |
|---|---|---|---|---|
| CT-ADR-001 | 2026-08-26 | 当前 Docs 以源项目功能为验收基线。 | 当前唯一需求是重构，没有经确认的新玩法。 | 不把推测性需求写入功能规格。 |
| CT-ADR-002 | 2026-08-26 | 每个功能采用“源行为／重构方式／状态／验收／证据”格式。 | 重构要能被独立复核。 | 功能矩阵与系统页必须有稳定 ID 和证据入口。 |
| CT-ADR-003 | 2026-08-26 | 采用分层 Docs，同时保留未来需求入口。 | 既要避免当前文档臃肿，也要避免未来改目录。 | 新需求先登记、决策、OpenSpec，再进入功能基线。 |
| CT-ADR-004 | 已有 OpenSpec | 采用项目通用 GF Loading UI Prefab。 | 避免 StartMenu 旧 Loading 与框架默认 Loading 重复。 | Loading 生命周期归 GF.UI／SceneFlow。 |
| CT-ADR-005 | 已有 OpenSpec | 暂不显示 Launch BuiltinView 默认 Loading。 | 当前没有启动动画或美术需求，裸进度条不构成产品体验。 | 保留错误对话框；未来按需求重新启用／替换。 |
| CT-ADR-006 | 已有 OpenSpec | 所有运行时创建／复用对象使用 `(Clone)` 后缀。 | 便于层级、日志与池复用诊断。 | 公共命名接口必须幂等。 |
| CT-ADR-007 | 2026-08-27 | Boss 场景不保留 UI 根或 UI 桥；战斗组合由 `BattleRuntimeContext (Clone)` 动态创建。 | UI 生命周期应从场景静态内容分离，切场景时能够统一释放。 | `BattleHud`、`BattleTutorial`、结果路由和演员绑定均由运行时所有者协调。 |
| CT-ADR-008 | 2026-08-27 | Launch 增加空的 `WorldUIRoot` Transform，而不是预置 World Space Canvas。 | 未来每种世界 UI 都应自行声明 Canvas／相机／缩放合同，避免无用途的全局 Canvas。 | 当前 Screen Space GF.UI 不挂入该根；未来动态附着后负责卸载。 |
| CT-ADR-009 | 2026-08-29 | 运行时性能采用 A+B：按需资源组合后直接精简 Hero AnimatorController 依赖。 | 单一 Controller 对全武器逐帧动画的引用是 Boss 内存峰值风险；仅压缩资源不足以解决常驻。 | 保留资源内容与事件合同，分阶段回归后降低 Boss2 峰值纹理内存。 |

原有两轮 Docs 建设讨论检查点见[方案讨论增量归档](方案讨论增量归档.md)。
