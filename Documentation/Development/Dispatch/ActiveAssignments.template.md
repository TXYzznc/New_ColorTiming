# 当前多窗口协作占用

> 维护者：制作人窗口
>
> 性质：非权威计划，只记录当前协作阶段、资源占用、阻塞与交接。

## 派发总览

| 派发 ID | 覆盖工作 | 专业窗口 | 协作状态 | 文件/目录占用 | 工具占用 | 阻塞与交接说明 |
|---|---|---|---|---|---|---|
| `<dispatch-id>` | `<work-identifiers>` | `<role-window>` | `<待领取/方案讨论/等待复核/可实施/实施中/待验收/暂停>` | `<paths-or-none>` | `<tools-or-none>` | `<handoff-or-none>` |

## 共享工具与 Git 占用

| 共享资源 | 当前占用者 | 状态 | 取得时间 | 释放条件/交接说明 |
|---|---|---|---|---|
| `<tool-or-git-index>` | `<role-window-or-none>` | `<占用/空闲>` | `<timestamp-or-none>` | `<release-condition>` |

> 删除示例行后再用于项目。不得在框架基线提交真实占用、窗口 ID、路径、端口或活动派发状态。
