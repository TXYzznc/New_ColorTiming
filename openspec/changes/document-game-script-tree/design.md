# 设计

## 目录说明

每个目录使用 `README.md`，保持内容简短：说明目录职责、直接脚本和直接子目录。父目录即使没有直接脚本，也必须说明它作为模块聚合边界的用途。

## 脚本注释

每个 `.cs` 文件以两行中文注释描述文件职责和所属模块。关键方法采用单行中文注释，优先解释生命周期、状态变化、依赖绑定和外部可调用行为；注释不复述语法细节。

## 安全约束

批处理保持 UTF-8 BOM 状态和原换行格式。验证器将工作树脚本与 `HEAD` 版本分别去除注释后比较，确保差异仅由注释构成，并检查 README、职责头和关键方法覆盖率。
