## 1. 设计

- [x] 1.1 确认攻击阶段由有效攻击事件或明确波次创建
- [x] 1.2 确认子技能默认继承父阶段
- [x] 1.3 确认新阶段的配置入口、默认值与迁移边界

## 2. 实现

- [x] 2.1 建立由执行 ID、阶段 ID、目标实例 Handle 与集中式投递服务构成的阶段结算边界
- [x] 2.2 为 `BattleDamage` 与 `Skill_base` 运行时接入阶段上下文；所有现有攻击根调用保持兼容地创建根阶段
- [ ] 2.3 建立 `CombatAttackDefinition`，并把子技能生成统一为显式阶段策略的 `SpawnChild` 请求
- [x] 2.4 迁移现有技能对象池释放路径并补充结构化命中日志；现有玩家武器、Boss 攻击根调用兼容接入

## 3. 验证

- [x] 3.1 覆盖同目标多 Collider 仅扣一次血
- [x] 3.2 覆盖两个独立伤害阶段可对同一逻辑目标分别结算
- [ ] 3.3 覆盖 AoE 对同一逻辑目标多个 Collider 仅结算一次；多目标仅以自动化夹具验证接口合同
- [ ] 3.4 覆盖继承阶段、显式新阶段与对象池复用
