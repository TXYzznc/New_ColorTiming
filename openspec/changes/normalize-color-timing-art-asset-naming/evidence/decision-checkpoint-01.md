# 决策检查点 01

日期：2026-08-28

## 已确认

- 整理范围是 `Assets/Game/Sprites/ColorTiming` 内全部资源和文件夹。
- 可以调整目录名称和层级，但不能修改或破坏美术内容。
- 目录采用英文 ASCII 与 PascalCase。
- 普通业务美术文件采用中文语义名称。
- Spine、SpriteAtlas、GF 资源组、配置键和代码资源 ID 使用英文 ASCII。
- 无把握的业务语义必须询问用户，不得自行推测。

## 尚未授权

- 尚未授权执行任何资源移动或重命名。
- 尚未确认含义不明资源和疑似测试资产的处理方式。

## 增量决定

- `Hero/白色.png` 保持原名，不参与重命名。
- 三个无外部引用的测试动画资产保留，迁入 `Hero/Tests` 并规范测试名称。
- 已确认无运行引用的效果参考图允许删除，原项目作为备份。
- Boss1 两套 Spine 的运行职责已由代码、场景和 JSON 事件交叉确认：主套为 `Core`，第二套为
  `Attack5` 专用套件，不得作为废弃资源删除。
- 两份第二关 Boss 拆分合成图 SHA-256 相同，且只存在未激活调试对象引用，不参与运行；按用户已授权
  的“无运行引用效果参考图可删除”规则纳入清理。
