# 运行时表现迁移证据索引

本目录保存项目 Loading UI、StartMenu BGM 所有权和运行时 `(Clone)` 命名变更的布局与验证证据。

- `ColorTimingLoading/prefab-layout.md`：Loading GF UI Prefab 结构。
- `ColorTimingLoading/editmode-results.xml`：针对 Loading 的 EditMode 结果。

完整运行时表现合同、任务状态和未完成的 PlayMode 验证见本 change 的 proposal、design、specs 与 tasks。
